# 02 — Decisions Log

| Date | Decision | Why | State left in |
|------|----------|-----|---------------|
| 2026-08-30 | Assess existing repo before any changes; clone + full code read, no modification | Working rules: understand before acting; research before feature debates | Baseline documented in `docs/CURRENT_STATE_REPORT.md`; continuity pack created |
| 2026-08-30 | Treat the original design doc as the product spec; the repo is a v2 prototype of it | The doc is the client requirement; repo is the partial implementation | Deviations tracked in `04_PLAN_DEVIATIONS.md` |
| 2026-08-30 | No code changed yet — assessment only; awaiting the user's original plan + approval before fixes | Release/approval protocol: nothing ships without explicit approval | Awaiting user |
| 2026-08-30 | **User confirmed the plan** = the pasted design doc; it's a **personal, local-only, single-user** job-hunting tool (not a deployed SaaS). Fixed `kuldeepyadav001/jobpilot` as source of truth. | Reduces auth priority; keeps everything else. | Local-only scope adopted |
| 2026-08-30 | **Deploy key provided.** SSH keypair generated in sandbox (`~/.ssh/id_ed25519`, private key OUTSIDE repo tree); public key pasted as repo **deploy key with write access**; remote switched to `git@github.com:...`; access verified. | Narrowest blast radius, revocable, single repo only. | Push access ready |
| 2026-08-30 | **Order of work:** fix bugs/breaking points first, features later (per user). Implemented Wave 1 correctness + deployment fixes. | Release protocol: report, then STOP and wait. | Wave 1 reported |
| 2026-08-30 | **Apply gate = Option A (user-confirmed):** user clicks the button once → full cycle runs itself (scrape→score→best resume→cover letter→apply→track). Confirmed as "I click manually, then everything done itself." | Gives the user explicit control over when applications actually go out; keeps scheduled runs non-applying to avoid surprise sends. | `APPLY_MODE` (real/dry_run) + `AUTO_APPLY` added |
| 2026-08-30 | **Wave 1 pushed** to `origin/main` at `1661593`. Deploy key used (write access, single repo). `git pull` on user side. | Release executed after gate passed (27 tests, ruff, build, migrations) | Live on `main` |
| 2026-08-30 | **UI reference supplied** (Edu Future dashboard + Perplexity landing). Product: JobPilot is a sellable product, so UI/feature/performance/security all matter. | Fused design: light EdTech-style dashboard + dark cinematic landing + light/dark toggle. | UI rebuilt 0-100 |
| 2026-08-30 | **Design tokens approach** — CSS vars + Tailwind `@theme` mapping; `dark` class on `<html>` toggled via localStorage. Light default (Edu Future), dark optional. | Keeps dark mode automatic + one styling vocabulary across all pages. | Tokens in `index.css` |
| 2026-08-30 | **UI release pushed** to `origin/main` at `87a1678` (Landing page, design system, all 8 pages retokened, dark toggle, apply top-N fix). Gate: 31 tests, lint, build, vite boot clean. | UI is sellable-looking and consistent. | Live on `main` |
