# 02 — Decisions Log

| Date | Decision | Why | State left in |
|------|----------|-----|---------------|
| 2026-08-30 | Assess existing repo before any changes; clone + full code read, no modification | Working rules: understand before acting; research before feature debates | Baseline documented in `docs/CURRENT_STATE_REPORT.md`; continuity pack created |
| 2026-08-30 | Treat the original design doc as the product spec; the repo is a v2 prototype of it | The doc is the client requirement; repo is the partial implementation | Deviations tracked in `04_PLAN_DEVIATIONS.md` |
| 2026-08-30 | No code changed yet — assessment only; awaiting the user's original plan + approval before fixes | Release/approval protocol: nothing ships without explicit approval | Awaiting user |
| 2026-08-30 | **User confirmed the plan** = the pasted design doc; it's a **personal, local-only, single-user** job-hunting tool (not a deployed SaaS). Fixed `kuldeepyadav001/jobpilot` as source of truth. | Reduces auth priority; keeps everything else. | Local-only scope adopted |
| 2026-08-30 | **Deploy key provided.** SSH keypair generated in sandbox (`~/.ssh/id_ed25519`, private key OUTSIDE repo tree); public key pasted as repo **deploy key with write access**; remote switched to `git@github.com:...`; access verified. | Narrowest blast radius, revocable, single repo only. | Push access ready |
| 2026-08-30 | **Order of work:** fix bugs/breaking points first, features later (per user). Implemented Wave 1 correctness + deployment fixes. | Release protocol: report, then STOP and wait. | Wave 1 reported |
| 2026-08-30 | **Apply gate = Option A (user-confirmed):** user clicks the button once → full cycle runs itself (scrape→score→best resume→cover letter→apply→track). Confirmed as "I click manually, then everything done itself." | Gives the user explicit control over when applications actually go out; keeps scheduled runs non-applying to avoid surprise sends. | `APPLY_MODE` (real/dry_run) + `AUTO_APPLY` added |
| 2026-08-30 | **Wave 1 pushed** to `origin/main` at `1661593`. Deploy key used (write access, single repo). `git pull` on user side. | Release executed after gate passed (27 tests, ruff, build, migrations) | Live on `main` |
| 2026-08-30 | **UI reference supplied** (Edu Future dashboard + Perplexity landing). Product: JobPilot is a sellable product, so UI/feature/performance/security all matter. | Fused design: light EdTech-style dashboard + dark cinematic landing + light/dark toggle. | UI rebuilt 0-100 |
| 2026-08-30 | **Design tokens approach** — CSS vars + Tailwind `@theme` mapping; `dark` class on `<html>` toggled via localStorage. Light default (Edu Future), dark optional. | Keeps dark mode automatic + one styling vocabulary across all pages. | Tokens in `index.css` |
| 2026-08-30 | **UI release pushed** to `origin/main` at `87a1678` (Landing page, design system, all 8 pages retokened, dark toggle, apply top-N fix). Gate: 31 tests, lint, build, vite boot clean. | UI is sellable-looking and consistent. | Live on `main` |
| 2026-08-30 | **Needs-Action flow designed** — `needs_manual_action` jobs do NOT consume the daily apply budget (was a bug); job marked handled to avoid duplicate "Needs Action" cards; UI now has Open Job + Mark Applied on the card & modal + an explanatory hint. | A manual-action item is not a submitted application; users must be able to act on it. | Needs-Action actionable |
| 2026-08-30 | **Started next stage (scraper reliability).** Added configurable `SEARCH_LOCATION`/`MAX_PER_PORTAL`; per-keyword try/except so one failed keyword/portal never aborts the cycle; new `/api/scrapers/diagnostics` dry-run endpoint + "Validate Scrapers" UI card. | Validate scrapers against live portals before trusting the pipeline. | Scraper harness built |
| 2026-08-30 | **Job auto-cleanup** (user asked to prune stale unapplied jobs weekly). Weekly cron deletes never-applied jobs older than `JOB_RETENTION_DAYS`; keeps applied + application-referenced jobs (never deletes "used" ones). + manual endpoint/button. | Keeps DB lean without losing candidate/applied jobs. | Cleanup live |
| 2026-08-30 | **Keyword strategy** for the user's 6 resumes set as default `SEARCH_KEYWORDS` (python/backend/full-stack/java/frontend+react/AI-ML/devops-cloud), broad net for volume. | Matches each resume role; cast wide for volume. | Default updated |
| 2026-08-30 | **Pipeline UI improved + removable columns** (toggle any column off, persisted to localStorage, "Columns" menu), column color dots + counts, filter. | Declutter the board to only relevant stages. | Pipeline UI upgraded |
| 2026-08-30 | **Score calibration (raise the %).** Coverage was diluted for generalist resumes (a 23-tag resume only matched ~35% even vs a perfect JD), compressing all scores low (~42 top). Fixed: coverage measured against a capped denominator (10) + rebalanced TF-IDF to 45%. Now scale spans 0 (irrelevant) → 100 (near-perfect), good match ~72, partial ~40s. Matcher + tests updated (9 matcher tests). | Make match scores reflect real fit so good jobs clear the apply threshold. | Scores recalibrated |
| 2026-08-30 | **Diagnostics "no output" fix (root cause: HTTP timeout).** Sync endpoint opened every job detail page in a headless browser (2s × up to 100 loads) > nginx 60s proxy timeout → blank. Fixed: scrapers get a lightweight `enrich=False` mode (listing-card data only, no per-job detail fetch) + diagnostics now runs as a BACKGROUND job via `POST /api/scrapers/diagnostics` then poll `GET /api/scrapers/diagnostics/status` (mirrors pipeline trigger). Settings UI starts + polls. | Validate scrapers against live portals without timing out. | Diagnostics backgrounded |
| 2026-08-30 | **Job vs Internship separation (user asked to keep them a little separate + I/J tags).** Added `Job.job_type` ("job"/"internship"), detected from the posting title by `detect_job_type()` (word-boundary regex for intern/internship/trainee/apprentice/co-op; guarded so "internal/international" don't false-positive). Alembic migration `b4e6c8d9a1f2`. Exposed in `JobOut`/`ApplicationOut`; `GET /api/jobs?job_type=` filter. Feed gets an **All / Jobs / Internships** toggle; J/I badges on Feed + Kanban cards. | Keep internships viewable/filterable separately. | job_type live |
| 2026-08-31 | **Performance / heat / lag (user: host gets too hot with multiple containers).** Root cause: `scrape_keyword` launched a NEW Chromium per keyword × portal (20 launches per 10-keyword cycle, every 6h) — huge CPU spike. Fixed by REUSING one browser per portal across the whole cycle (`ensure_page` API + `close_browser_on_scrape` flag), closing once at the end. Also: Docker resource limits (cpu/mem) on every service so one container can't saturate the host; Prometheus+Grafana moved behind a `monitoring` compose profile (no longer started by default); Ollama tuned (`OLLAMA_NUM_PARALLEL=1`, `MAX_LOADED_MODELS=1`, `KEEP_ALIVE=5m`). | Cut CPU/heat substantially on a personal single-user host. | Browser reuse + limits |
